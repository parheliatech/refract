"""Refract shell: root launcher scene, status overlay, HUD navigator.

Built in Phases 2-3. The root screen is launcher tiles ONLY -- configuration
belongs in the HUD (per-container settings or Global Settings), never here.
"""
